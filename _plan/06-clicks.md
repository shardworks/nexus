# Click IDs Found

## c-mod9a54n

- **Location:** inventory (Scope and blast radius / Reckoner consumer bullet)
  - **Context:** "- **Reckoner consumer** — the Reckoner (per `c-mod9a54n`) plans to declare `{ \"schedule\": \"@every <interval>\", \"run\": \"reckoner-tick\" }`. No code change here, but the shape must be exactly what that consumer expects."
- **Location:** inventory (Existing context section)
  - **Context:** "- The Reckoner's MVP design (`c-mod9a54n` conclusion) is the load-bearing consumer: \"Interval is Clockworks config\" — the Reckoner installs a scheduled standing order pointing at its own tick relay."

## Notes

- No click IDs (matching the `c-XXXXX` pattern) were found in any decision rationale, option, or question fields.
- No click IDs were found in any scope description or rationale fields.
- No click IDs were found in any observation field.
- The substring `c-refresh` appears in the inventory but only as part of the literal token `doc-refresh` (e.g. "doc-refresh commission") — not a click id.
