/**
 * Superseded by HandlerContext in plugin.ts.
 * Re-exported here under both names for import-path continuity during transition.
 */
export type { HandlerContext, HandlerContext as RigContext } from './plugin.ts';
