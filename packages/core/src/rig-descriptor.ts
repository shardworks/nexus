/**
 * Superseded. Rig dependencies were declared in rig.json and validated at
 * install time. The plugin model declares dependencies via `requires` on the
 * Kit or Apparatus manifest and validates them at guild startup.
 *
 * See: docs/architecture/plugins.md#dependencies
 */
